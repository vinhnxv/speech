openai
numpy
requests
soundfile
torch
torchaudio
transformers>=5.4.0
sentencepiece
protobuf
librosa
# pillow + torchvision + accelerate: needed by Gemma 4 multimodal processor
pillow
torchvision
accelerate

# Use a separate virtualenv per README (own dependency stacks, not installable here):
#   qwen-asr      (pins transformers==4.57.6)  → pyenv virtualenv 3.14.5 qwen && pip install qwen-asr
#   nemo_toolkit  (wants transformers 4.53.x)  → pyenv virtualenv 3.14.5 nemo && pip install "nemo_toolkit[asr]"
#   vieneu        (torch-free ONNX; onnxruntime/gradio/sea-g2p) → pyenv virtualenv 3.14.5 vieneu && pip install vieneu
